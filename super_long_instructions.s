@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
@ This is just a super duper long
@ fake assembly program to test the assembler.
@ This doesn't do anything...
ADDS: R0, R0, R0
ADDS: R0, R0, #1
SUB: R1, R1, R1
MOV: R1, #0
CMP: R0, #64
PRNR: R0, uint
